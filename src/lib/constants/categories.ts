/**
 * Placeholder category list for the Beauty & Cosmetics launch market.
 * Per the PRD, categories must be configurable rather than hardcoded —
 * this file stands in for what should eventually come from the backend
 * (an admin-managed list), not a permanent fixed enum.
 */
export const PRODUCT_CATEGORIES = [
  "Skincare",
  "Haircare",
  "Makeup",
  "Fragrance",
  "Personal Care",
] as const;